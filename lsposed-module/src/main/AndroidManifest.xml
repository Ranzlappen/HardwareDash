<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="HardwareDash GPS Spoofer"
        android:hasCode="true">

        <!-- LSPosed module metadata. Recognized by both classic Xposed
             (de.robv.android.xposed.installer) and LSPosed (org.lsposed.manager). -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Hides isFromMockProvider() / isMock() and the MOCK_LOCATION AppOp from third-party apps. Pair with HardwareDash GPS spoofing." />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposedscope" />

    </application>

</manifest>
