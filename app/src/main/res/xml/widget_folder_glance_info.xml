<?xml version="1.0" encoding="utf-8"?>
<!--
  Glance flavor of the folder widget. Wider than the 2x2 RemoteViews variant
  (3x2 target cells) so the two flavors visibly differ in the widget picker.
  Same configure activity reads the size from the registered provider's
  ComponentName at config time.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="80dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_folder_2x2"
    android:configure="com.gadget.widget.folder.FolderWidgetConfigActivity"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/widget_folder_2x2"
    android:description="@string/widget_folder_glance_desc" />
