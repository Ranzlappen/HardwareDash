<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (the Compose code overrides most of this) -->
    <style name="Theme.Gadget" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Floating, transparent variant for FolderPopupActivity. The system
         finishes the activity automatically when the user taps outside the
         floating window thanks to windowCloseOnTouchOutside=true. -->
    <style name="Theme.Gadget.Translucent" parent="Theme.Gadget">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
