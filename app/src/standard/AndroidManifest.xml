<?xml version="1.0" encoding="utf-8"?>
<!--
    Standard (non-rooted) flavor manifest fragment. Intentionally empty —
    the standard build adds nothing on top of src/main/AndroidManifest.xml.
    Any rooted-only permission, <queries>, provider, or service must live in
    src/rooted/AndroidManifest.xml so it never reaches the standard APK.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
