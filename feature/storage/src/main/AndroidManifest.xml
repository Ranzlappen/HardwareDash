<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Internal-storage home-screen widget (content/display archetype).
             Display-only: no APPWIDGET_CONFIGURE activity. -->
        <receiver
            android:name=".widget.StorageWidgetProvider"
            android:exported="true"
            android:label="@string/storage_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_storage_info" />
        </receiver>
    </application>

</manifest>
