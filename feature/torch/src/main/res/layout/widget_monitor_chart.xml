<?xml version="1.0" encoding="utf-8"?>
<!--
  Monitor chart widget layout — RemoteViews-safe classes only (LinearLayout,
  FrameLayout, TextView, ImageView, Button, ImageButton). The chart itself is
  a Bitmap pushed into the ImageView via setImageViewBitmap (RemoteViews can't
  host a Compose/Vico chart). See the RemoteViews note in CLAUDE.md: a bare
  <View>/<Space> would throw InflateException.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background_glass"
    android:padding="10dp">

    <TextView
        android:id="@+id/widget_chart_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/widget_monitor_chart_label"
        android:textColor="@android:color/white"
        android:textStyle="bold" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="6dp">

        <ImageView
            android:id="@+id/widget_chart_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:contentDescription="@string/widget_monitor_chart_label"
            android:scaleType="fitXY" />

        <TextView
            android:id="@+id/widget_chart_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/widget_monitor_chart_empty"
            android:textColor="@android:color/white"
            android:visibility="gone" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/widget_chart_toggle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/widget_monitor_toggle_off" />

        <ImageButton
            android:id="@+id/widget_chart_reload"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/widget_monitor_reload"
            android:src="@android:drawable/ic_popup_sync" />
    </LinearLayout>
</LinearLayout>
