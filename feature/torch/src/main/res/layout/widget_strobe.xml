<?xml version="1.0" encoding="utf-8"?>
<!--
  Strobe widget layout. Same structure as widget_flashlight.xml —
  background surface + icon image + transparent button wrapper that
  receives the click PendingIntent.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp">

    <ImageView
        android:id="@+id/widget_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:contentDescription="@null" />

    <ImageButton
        android:id="@+id/widget_strobe_button"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:contentDescription="@string/widget_strobe_label"
        android:padding="12dp"
        android:scaleType="fitCenter"
        android:src="@android:color/transparent" />

    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:padding="12dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_strobe" />

</FrameLayout>
