<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Ambient-light home-screen widget (content/display archetype).
             Display-only: no APPWIDGET_CONFIGURE activity. -->
        <receiver
            android:name=".widget.AmbientWidgetProvider"
            android:exported="true"
            android:label="@string/ambient_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_ambient_info" />
        </receiver>
    </application>

</manifest>
