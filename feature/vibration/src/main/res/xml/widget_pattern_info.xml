<?xml version="1.0" encoding="utf-8"?>
<!-- AppWidget metadata for the legacy pattern widget — a 1×1 tap target that
     resizes up so the adaptive render can show the name label. Kept registered
     only for already-placed pattern widgets (new pins use the vibrate widget). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:minResizeWidth="40dp"
    android:minResizeHeight="40dp"
    android:maxResizeWidth="250dp"
    android:maxResizeHeight="250dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_pattern"
    android:previewLayout="@layout/widget_pattern"
    android:previewImage="@drawable/ic_vibration_pattern"
    android:description="@string/widget_pattern_description"
    android:widgetCategory="home_screen"
    android:resizeMode="horizontal|vertical" />
